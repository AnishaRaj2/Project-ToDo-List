import Button from "./Button";

export default function Addtask () {
    
    return (
        <form className="flex">
            <input type="text" placeholder="Add new task"/> 
            <Button />
            </form> 
    );
}